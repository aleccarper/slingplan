<%= section_wrap_full({
  title: "Settings",
  subtitle: "Make SlingPlan work just the way you'd like."
}) do %>
  Settings and customizations will evolve as we build consensus of what users really want based on feedback.
<% end %>



<div class='inner'>
  <%= responsive_form_for(@settings, url: planners_admin_account_settings_path, method: :patch) do |f| %>
    <div class='subsection-wrap'>
      <h3>Email Frequency</h3>
      <div class='hint'>
         How often would you like to receive bids and messages from vendors, as well as responses to support tickets?
      </div>
      <%= f.select :email_frequency,
        collection: Settings.email_frequencies.keys.map { |f| [f.titleize, f] },
        label: false
      %>
    </div>

    <%= f.submit 'Save' %>
  <% end %>
</div>

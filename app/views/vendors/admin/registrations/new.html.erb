<%= section_wrap_full({
  title: "Vendor Account",
  subtitle: ("Do you already have a SlingPlan account?&nbsp;&nbsp;" + link_to("Sign in", new_session_path(resource_name), class: 'btn sign-in')).html_safe,
  class: 'short'
}) do; end %>



<div class='inner devise'>
  <%= responsive_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

    <div class='hint'>
      <h4>The Service Providers</h4>
      <br />
      Advertise your services and get in direct contact with meeting planners by signing up with a Vendor account. By signing up as a vendor, event planners will be able to contact you directly detailing the services they need and the price they are looking to pay.  As an added bonus - have your locations show on our map with all your contact info and boosted search rankings.

      <br />
      <br />
      <h6>you will not be billed until you recieve your first Request For Proposal!</h6>
    </div>

    <div class='subsection-wrap'>
      <div class='subtitle'>Account Credentials</div>
      <div class='section-content'>
        <div class='hint'>
          This is the log in information you will use to sign into SlingPlan.
        </div>
        <%= f.email_field :email, required: true %>
        <%= f.password_field :password, autocomplete: "off", required: true %>
        <%= f.password_field :password_confirmation, autocomplete: "off", required: true %>
      </div>
    </div>

    <div class='subsection-wrap'>
      <div class='subtitle'>Who you are</div>
      <div class='section-content'>
        <div class='hint'>
          Future customers will use this information to contact you.
        </div>
        <%= f.text_field :name, label: 'Company Name', required: true %>
        <%= f.text_field :primary_phone_number %>
        <%= f.text_field :primary_email %>
        <%= f.text_field :primary_website %>
      </div>
    </div>


    <div class='subsection-wrap'>
      <div class='subtitle'>What you do</div>
      <div class='section-content'>
        <div class='hint'>
          Please fill out a description of what it is you do and what services you provide.
        </div>

        <%= f.text_area :description %>
      </div>

      <div class='subtitle'>Misc</div>
      <div class='section-content'>
        <%= f.time_zone_select :time_zone,
          ActiveSupport::TimeZone.us_zones,
          default: 'Central Time (US & Canada)'
        %>
        <%= f.file_field :logo_image, name: 'vendor[logo_image]', label: 'Logo Image (.jpg, .png, .gif, .bmp)' %>
      </div>
    </div>

    <div class='accept-tos-wrap'>
      <span>
        <%= f.check_box :accepted_terms_of_service %>
        <%= f.label :accepted_terms_of_service, 'Accept ' %>
      </span>

      <span>
        <a href='/legal/terms' target='_blank'>Terms of Service</a>
      </span>
    </div>

    <%= f.submit "Next" %>

  <% end %>
</div>
